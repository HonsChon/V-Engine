# 图形踩坑记录

---

## 1. Vulkan Y 轴翻转导致 SSAO 屏幕空间半径计算错误

**日期**：2026-04-27

**现象**：SSAO 开关后渲染效果完全没有变化，捉帧发现 reinterleave 输出的 AO 纹理全是 1.0（无遮蔽）。

### 背景

从深度比较方式的 SSAO 切换到 Alchemy AO 风格后，需要在 shader 中根据像素深度计算**屏幕空间采样半径**（世界空间半径 → 像素数），公式为：

```glsl
screenRadius = (worldRadius / |viewDepth|) * focalLength * 0.5 * subTextureSize
```

其中 `focalLength` 来自投影矩阵：
- `projection[0][0]`：X 轴焦距
- `projection[1][1]`：Y 轴焦距

### 根因

**OpenGL 和 Vulkan 的 NDC Y 轴方向相反。**

| | NDC Y 轴 | 屏幕原点 |
|---|---|---|
| OpenGL | ↑ 向上 | 左下角 (-1,-1) |
| Vulkan | ↓ 向下 | 左上角 (-1,-1) |

因为项目使用 `glm::perspective`（OpenGL 风格），需要手动翻转 Y 轴来适配 Vulkan：

```cpp
glm::mat4 proj = glm::perspective(fov, aspect, 0.1f, 100.0f);
proj[1][1] *= -1;  // 适配 Vulkan NDC
```

`glm::perspective` 生成的 `projection[1][1]` 原本为正数（`1 / tan(fov/2)`，如 FOV=45° 时约 2.414），乘以 -1 后变为 **负数**。

在 SSAO shader 中直接使用这个负值：

```glsl
// ❌ 错误代码
float screenRadiusX = (radius / abs(P.z)) * ubo.projection[0][0] * 0.5 * subWidth;   // 正数
float screenRadiusY = (radius / abs(P.z)) * ubo.projection[1][1] * 0.5 * subHeight;  // 负数！

float avgScreenRadius = (screenRadiusX + screenRadiusY) * 0.5;
// 例: (20.8 + (-20.8)) / 2 = 0.0

if (avgScreenRadius < 1.0) {
    outAO = 1.0;  // ← 所有像素都走了这里！
    return;
}
```

`screenRadiusX` 为正、`screenRadiusY` 为负，两者相加接近 0，导致所有像素触发 early return，AO 全部输出 1.0。

### 修复

取绝对值——计算屏幕空间半径只关心焦距的**大小**，不关心坐标轴方向：

```glsl
// ✅ 正确代码
float screenRadiusX = (radius / abs(P.z)) * abs(ubo.projection[0][0]) * 0.5 * subWidth;
float screenRadiusY = (radius / abs(P.z)) * abs(ubo.projection[1][1]) * 0.5 * subHeight;
```

### 为什么旧版（深度比较方式）没有这个问题

旧版使用 3D 半球 TBN 采样，在 view space 中直接偏移 3D 坐标，然后将偏移后的点通过投影矩阵投影回屏幕坐标去查深度：

```glsl
vec4 offset = projection * vec4(samplePos, 1.0);
offset.xyz /= offset.w;
offset.xyz = offset.xyz * 0.5 + 0.5;  // NDC → UV
```

这个流程中 Y 轴翻转是**需要的**（因为最终要映射到 Vulkan 的屏幕 UV 空间），所以旧版恰好不受影响。

### 教训

> 当投影矩阵的某个分量被用于**反算几何量**（如屏幕空间尺寸、焦距）而非正常投影变换时，必须注意 Vulkan Y 翻转带来的符号问题，使用 `abs()` 取绝对值。

---
